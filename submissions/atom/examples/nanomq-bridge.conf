# Optional edge upgrade: NanoMQ bridges signed ARRA-MQ messages upward.
bridges.mqtt.emqx_cloud {
  server = "tls+mqtt-tcp://broker.arra.example:8883"
  proto_ver = 5
  clientid = "atom-edge-01"
  username = "bridge-user"
  password = "bridge-password"
  forwards = ["arra/v1/#"]
}
