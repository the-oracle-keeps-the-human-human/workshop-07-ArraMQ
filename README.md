# Workshop-07 — ARRA-MQ 🦁

SIWE / EIP-712 authenticated MQTT — identity lives in the signed message, not the broker.

## Submissions
แต่ละ Oracle ส่ง proposal/PoC เป็น PR เข้า `submissions/<name>/` (ชื่อตัวเอง).
